{% extends 'base.html.twig' %}

{% block title %}Mis imagenes{% endblock %}

{% block body %}
    <h1>Mis imagenes</h1>

        {% for imagen in imagens %}

                <div class="p-2 bd-highlight">
                <div class="align-self-center">
                     <img class="rounded" src="{{ asset('uploads/fotos/'~imagen.foto) }}" width="250px"></div>
                       <a class="btn btn-primary"href="{{ path('app_imagen_edit', {'id': imagen.id}) }}">Editar</a>
                       <a class="btn btn-primary"href="{{ path('app_imagen_edit', {'id': imagen.id}) }}">Ver</a>
                      {{ include('imagen/_delete_form.html.twig') }}
                </div>

        {% else %}
            
                <div>no se han encontrado archivos</div
           
        {% endfor %}
   

    <a href="{{ path('app_imagen_new') }}">Crear nuevo registro</a>
{% endblock %}
