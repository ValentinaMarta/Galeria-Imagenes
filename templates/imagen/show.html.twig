{% extends 'base.html.twig' %}

{% block title %}Imagen{% endblock %}

{% block body %}
    <h1>{{ imagen.titulo }}</h1>
               
                <div class="cajab">
                     <img class="rounded" src="{{ asset('uploads/fotos/'~imagen.foto) }}" width="400px"></div>
               
                </div>
           
                <div class="cajab">  
                          <a class="btn" href="{{ path('app_imagen_edit', {'id': imagen.id}) }}">Editar</a>
                            {{ include('imagen/_delete_form.html.twig') }}
                          <a class="btn "href="{{ path('app_imagen_index') }}">Volver a la lista</a>
              <div>
{% endblock %}
